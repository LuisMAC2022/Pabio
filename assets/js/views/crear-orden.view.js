import {crearOrden} from '../modules/ordenes.js';
export function renderCrearOrden(root){root.innerHTML=`<form id="f-orden"><label for="cliente">Cliente</label><input id="cliente" required><label for="material">Material</label><input id="material" required><label for="cantidad">Cantidad</label><input id="cantidad" type="number" min="1" required><button class="btn" type="submit">Guardar orden</button></form><p id="msg"></p>`;root.querySelector('#f-orden').addEventListener('submit',e=>{e.preventDefault();const cliente=root.querySelector('#cliente').value;const material=root.querySelector('#material').value;const cantidad=Number(root.querySelector('#cantidad').value);const orden=crearOrden({cliente,materiales:[{material,cantidad}]});root.querySelector('#msg').textContent=`Orden creada: ${orden.folio}`;});}
